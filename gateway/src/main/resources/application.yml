spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://172.17.1.3:3306/zk_net?useSSL=false&zeroDateTimeBehavior=convertToNull&autoReconnect=true&allowMultiQueries=true&tinyInt1isBit=false
    username: baas
    password: B1ockch@in
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      stat-view-servlet:
        login-username: zknet-admin
        login-password: zknetHzvWYDPD
  redis:
    database: 6
    host: 172.17.1.3
    port: 6379
    password: B1ockch@in@redis
    timeout: 60s
    lettuce:
      pool:
        max-active: 8
        max-wait: -1s
        max-idle: 8
        min-idle: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml #sql映射文件的位置
  configuration:
    map-underscore-to-camel-case: true #驼峰命名开启
  type-aliases-package: com.zknet.www.demo.api.entity #实体类所在的包，自动扫描并映射实体类

logging:
  config: classpath:log4j2-test.xml
server:
  port: 8088
